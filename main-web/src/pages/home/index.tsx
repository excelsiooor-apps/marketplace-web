import { FC } from 'react';
import { MainContainer } from '@/components/app-containers';

const HomePage: FC = () => {
  return (
    <MainContainer>
      <h1>Hello!</h1>
    </MainContainer>
  );
};

export default HomePage;
